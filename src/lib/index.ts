// Shared infrastructure. No business logic, no UI.
// Contains: api.client.ts (axios instance + interceptors), queryClient.ts (TanStack config), utils.ts (formatCurrency, formatDate, getDaysUntilDue), router.tsx (React Router config).
